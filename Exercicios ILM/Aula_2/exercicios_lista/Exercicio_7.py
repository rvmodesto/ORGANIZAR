#Fornecido um número inteiro n (n ≥ 0), exibir a raiz quadrada desse valor, para tanto utilize o operador
#de potência (∗∗).

import math

n = int(input('Digite o número: '))

raiz = math.sqrt(n)

print('A raiz quadrada de ', n, 'é: ', raiz)
